const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=(file)=>fs.readFileSync(path.join(root,file),'utf8');
test('platform merchant management is routed with full safe CRUD and invitation controls',()=>{const router=read('src/router/index.js'),nav=read('ui-config.js'),api=read('src/api/admin-merchants.js'),view=read('src/views/platform/MerchantsView.vue');assert.match(router,/MerchantsView/);assert.match(nav,/platform-merchants/);assert.match(api,/method:\s*'POST'/);assert.match(api,/method:\s*'PUT'/);assert.match(api,/method:\s*'DELETE'/);assert.match(api,/invitation-code/);assert.match(view,/<table/);assert.match(view,/confirmAction/);assert.match(view,/新增商户/);});
