<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.familykitchen.user.mapper.UserRoleMapper">
  <insert id="insert">
    INSERT INTO user_role_relations(user_id,role_code,status) VALUES(#{userId},#{roleCode},'ACTIVE')
  </insert>
  <select id="findActiveRoles" resultType="string">
    SELECT role_code FROM user_role_relations WHERE user_id=#{userId} AND status='ACTIVE'
  </select>
  <select id="countRole" resultType="int">
    SELECT COUNT(*) FROM user_role_relations WHERE user_id=#{userId} AND role_code=#{roleCode} AND status='ACTIVE'
  </select>
</mapper>
