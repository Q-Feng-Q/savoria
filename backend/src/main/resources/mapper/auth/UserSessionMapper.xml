<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.familykitchen.auth.mapper.UserSessionMapper">
  <insert id="insert">INSERT INTO user_sessions(id,user_id,token_hash,status,expires_at)
    VALUES(#{id},#{userId},#{tokenHash},'ACTIVE',DATE_ADD(NOW(),INTERVAL 2 HOUR))</insert>
  <select id="countActive" resultType="int">SELECT COUNT(*) FROM user_sessions WHERE id=#{id}
    AND user_id=#{userId} AND status='ACTIVE' AND expires_at&gt;NOW()</select>
  <update id="revoke">UPDATE user_sessions SET status='REVOKED',revoked_at=NOW()
    WHERE id=#{id} AND user_id=#{userId} AND status='ACTIVE'</update>
  <update id="revokeAll">UPDATE user_sessions SET status='REVOKED',revoked_at=NOW()
    WHERE user_id=#{userId} AND status='ACTIVE'</update>
</mapper>
