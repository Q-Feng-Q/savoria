const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');
test('login and registration actions share one responsive row',()=>{const wxml=fs.readFileSync(path.join(root,'pages/auth/entry/index.wxml'),'utf8');const wxss=fs.readFileSync(path.join(root,'pages/auth/entry/index.wxss'),'utf8');assert.match(wxml,/entry-actions/);assert.match(wxml,/submitLogin[\s\S]*openRegister/);assert.match(wxss,/\.entry-actions\s*\{[^}]*display:\s*grid/);assert.match(wxss,/grid-template-columns/);});
