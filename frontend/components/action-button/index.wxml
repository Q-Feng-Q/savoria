<view class="ui-button warm-component warm-action-button action-button action-button--{{tone}} {{block ? 'action-button--block' : ''}} {{disabled || loading ? 'is-disabled' : ''}} {{loading ? 'is-loading' : ''}}" aria-role="button" aria-label="{{loading ? loadingLabel : label}}" aria-disabled="{{disabled || loading}}" aria-busy="{{loading}}" hover-class="ui-button--pressed" hover-stay-time="80" bindtap="handleTap"><text>{{loading ? loadingLabel : label}}</text><slot></slot></view>
